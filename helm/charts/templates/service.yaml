apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.workloadName }}-service
  labels:
    {{- include "wedding-invitation-react-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: {{ .Values.service.protocol }}
  selector:
    {{- include "wedding-invitation-react-chart.selectorLabels" . | nindent 4 }}
